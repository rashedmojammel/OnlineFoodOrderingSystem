@model List<BAL.DTOs.FoodDTO>
@{
    ViewData["Title"] = "Manage Food Items";
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        <i class="fa-solid fa-circle-check me-2"></i>@TempData["Success"]
    </div>
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>
        <i class="fa-solid fa-burger me-2" style="color:#ff6b35"></i>
        Manage Food Items
    </h2>
    <div>
        <a asp-action="Create" class="btn btn-success me-2">
            <i class="fa-solid fa-plus me-1"></i> Add Food
        </a>
        <a asp-controller="AdminDashboard" asp-action="Index"
               class="btn btn-secondary">
            <i class="fa-solid fa-arrow-left me-1"></i> Back to Dashboard
        </a>
    </div>
</div>

<!-- Search -->
<form method="get" asp-action="Index" class="mb-3">
    <div class="input-group">
        <span class="input-group-text" style="background:#ff6b35; color:white;">
            <i class="fa-solid fa-magnifying-glass"></i>
        </span>
        <input type="text" name="search" value="@ViewBag.Search"
               class="form-control" placeholder="Search food items..." />
        <button type="submit" class="btn btn-success">
            <i class="fa-solid fa-magnifying-glass me-1"></i> Search
        </button>
        @if (ViewBag.Search != null)
        {
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fa-solid fa-xmark me-1"></i> Clear
            </a>
        }
    </div>
</form>

@if (ViewBag.Search != null)
{
    <p class="text-muted mb-3">
        <i class="fa-solid fa-circle-info me-1"></i>
        Found <strong>@Model.Count</strong> result(s) for "@ViewBag.Search"
    </p>
}

<div class="card border-0"
     style="border-radius:15px; box-shadow:0 4px 20px rgba(0,0,0,0.08)">
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead style="background:#ff6b35; color:white;">
                <tr>
                    <th class="ps-4">
                        <i class="fa-solid fa-hashtag me-1"></i>ID
                    </th>
                    <th><i class="fa-solid fa-burger me-1"></i>Name</th>
                    <th>
                        <i class="fa-solid fa-align-left me-1"></i>Description
                    </th>
                    <th>
                        <i class="fa-solid fa-money-bill me-1"></i>Price
                    </th>
                    <th>
                        <i class="fa-solid fa-utensils me-1"></i>Category
                    </th>
                    <th><i class="fa-solid fa-gear me-1"></i>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count == 0)
                {
                    <tr>
                        <td colspan="6" class="text-center text-muted py-4">
                            <i class="fa-solid fa-face-frown fa-2x mb-2 d-block"></i>
                            No food items found
                        </td>
                    </tr>
                }
                else
                {
                    @foreach (var food in Model)
                    {
                        <tr>
                            <td class="ps-4">@food.Id</td>
                            <td><strong>@food.Name</strong></td>
                            <td class="text-muted">
                                @(food.Description ?? "No description")
                            </td>
                            <td>
                                <span class="fw-bold" style="color:#ff6b35">
                                    ৳@food.Price
                                </span>
                            </td>
                            <td>
                                <span class="badge bg-success">
                                    <i class="fa-solid fa-utensils me-1"></i>
                                    @food.CategoryName
                                </span>
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@food.Id"
                                       class="btn btn-warning btn-sm">
                                    <i class="fa-solid fa-pen-to-square me-1"></i>Edit
                                </a>
                                <a asp-action="Delete" asp-route-id="@food.Id"
                                       class="btn btn-danger btn-sm">
                                    <i class="fa-solid fa-trash me-1"></i>Delete
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>